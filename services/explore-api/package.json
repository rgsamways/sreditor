{
  "name": "sreditor-explore-api",
  "version": "0.0.1",
  "private": true,
  "description": "Backend for `sreditor explore`: a periodic refresh job that clusters raw field-trend items (arXiv, GitHub trending, HN, changelogs) into cards via an LLM pass, and a read-only endpoint the CLI polls. Stores no developer or project identity -- every requester sees the same corpus.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "migrate": "node dist/migrate.js",
    "refresh": "node dist/refresh.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "fast-xml-parser": "^4.5.3",
    "pg": "^8.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/pg": "^8.11.10",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
